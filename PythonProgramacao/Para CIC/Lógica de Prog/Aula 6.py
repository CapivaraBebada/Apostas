'''
    Estrutura de decisão simples
        se, então - if

    Estrutua de decisão composta
        se, então, se não - if, else

    Se o que foi programadofor correto, então será usado o if e se não
for, então usa-se o else.

    O encadeamento é o quando vc quer que o seu código seja lido dentro
do if ou do else, dessa forma, se for aceito as condições para o "if" o
código dele será lido, mas se não, o do "else" será lido.

'''